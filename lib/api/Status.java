package lib.api;

public enum Status {
        OK,
        NO_SUCH_USER,
        BAD_PASSWORD,
        USER_EXISTS,
        MUST_LOGIN,
        NO_SUCH_CITY,
        NO_SUCH_HOTEL,
        ALREADY_LOGGED,
        NOT_LOGGED,
        SCORE_GLOBAL,
        SCORE_POSITION,
        SCORE_CLEANING,
        SCORE_PRICE,
        SCORE_SERVICE;  
}
